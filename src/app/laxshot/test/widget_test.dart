import 'package:flutter_test/flutter_test.dart';
import 'package:laxshot/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: wire up Firebase test mocks before running app widget tests
  });
}
